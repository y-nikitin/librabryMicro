spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:db}:${DB_PORT:5432}/notification
    username: ${DB_USERNAME:notification_svc}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
