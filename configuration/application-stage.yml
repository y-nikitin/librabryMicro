spring:
  rabbitmq:
    host: ${RABBITMQ_HOST:rabbitmq}
    port: 5672

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh,busrefresh

logging:
  level:
    root: INFO
