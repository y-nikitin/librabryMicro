server:
  port: 8082

spring:
  application:
    name: borrowing-service
  datasource:
    url: jdbc:postgresql://localhost:5432/borrow
    username: postgres
    password: 12345
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

book-service:
  base-url: http://localhost:8081
  books-path: /api/v1/books